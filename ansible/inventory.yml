all:
  hosts:
    local:
      localhost:
        ansible_connection: local
    remote:
      rpi:
        ansible_host: 192.168.0.171
        ansible_ssh_private_key_file: ~/.ssh/id_ed25519
  vars:
    ansible_user: rpi
    path_to_repo: /home/{{ ansible_user }}/actions_repos/rpi-virtualisation/rpi-virtualisation/