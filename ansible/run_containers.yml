- name: run containers roles to set up services
  hosts: localhost
  roles:
    - role: vpn
      tags: [vpn]
    - role: klipper
      tags: [klipper]
    - role: samba #samba is started last so it can get latest config
      tags: [samba] 
